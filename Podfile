# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Var_SwiftCleanArcitecture' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Var_SwiftCleanArcitecture
  pod 'Alamofire'
  pod 'SwiftyJSON'
  target 'Var_SwiftCleanArcitectureTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Var_SwiftCleanArcitectureUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
